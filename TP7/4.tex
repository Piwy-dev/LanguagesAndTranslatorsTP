\begin{center}
    $x\cdot2 = x+x$ or  $x\verb|<<|1$ \\
    $x\cdot3 = x+x+x$ or  $x\verb|<<|1 + x$ \\
    $x\cdot4 = x\verb|<<|2$ \\
    $x\cdot5 = x\verb|<<|2 + x$ \\
    $x\cdot(2^n + 1) = (x\verb|<<|n) + x$ \\
    $x\cdot(2^n - 1) = (x\verb|<<|n) - x$
\end{center}
In general, we can also combine these rules to get more complex multiplications. For example, $x\cdot13 = x\cdot(8 + 4 + 1) = (x\verb|<<|3) + (x\verb|<<|2) + x$.