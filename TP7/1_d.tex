ARM-like machine code:
\begin{lstlisting}[language = Java , frame = trBL, escapeinside={(*@}{@*)}]
mul r0, r1, 2
add r2, r0, 1
add r1, r1, r2
mul r2, r2, 2
add r0, r2, r0
add r0, r1, r0
return r0
\end{lstlisting}